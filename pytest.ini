[pytest]
# Tests import the backend as `src.backend.*` — the same path Uvicorn uses
# for `src.backend.main:app`. See tests/conftest.py.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q
